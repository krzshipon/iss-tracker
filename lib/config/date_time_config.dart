/// Centralized configuration for date and time formats.
class DateTimeConfig {
  /// Default date and time format (e.g., "2023-12-25 14:30:45").
  static const String dateTimeFormat = 'yyyy-MM-dd HH:mm:ss';

  /// Date-only format (e.g., "2023-12-25").
  static const String dateOnlyFormat = 'yyyy-MM-dd';

  /// Time-only format (e.g., "14:30:45").
  static const String timeOnlyFormat = 'HH:mm:ss';
}
